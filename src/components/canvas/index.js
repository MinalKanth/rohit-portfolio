import EarthCanvas from "./Earth";
import BallCanvas from "./Ball";
import ComputersCanvas from "./Computers";
import StarsCanvas from "./Stars";

/*
 * Centralized 3D experience layer
 *
 * EarthCanvas    → Contact / global availability
 * BallCanvas     → Interactive technology icons
 * ComputersCanvas → Hero 3D Mac / developer workspace
 * StarsCanvas    → Global cinematic background
 */

export {
  EarthCanvas,
  BallCanvas,
  ComputersCanvas,
  StarsCanvas,
};